name: app_package
description: appsパッケージを作成するbrick

version: 0.1.0

environment:
  mason: ">=0.1.0"

vars:
  app_name:
    type: string
    description: App name
    prompt: 'What is app name? (example: foo_bar)'
