import 'package:flutter/material.dart';

class TestableKeyApp extends StatelessWidget {
  const TestableKeyApp({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}
